{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"dockerBuild": {
				"tag": "serviceauth:latest",
				"dockerfile": "Dockerfile",
				"context": "${workspaceFolder}/services/api",
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"command": "ls"
				
			}

		}
	]
}